import 'package:flutter/material.dart';

class StudentsLevelOneScreen extends StatelessWidget {
  const StudentsLevelOneScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
