import 'package:flutter/material.dart';

class StudentsLevelTwoScreen extends StatelessWidget {
  const StudentsLevelTwoScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
