import 'package:flutter/material.dart';

class StudentsLevelSixScreen extends StatelessWidget {
  const StudentsLevelSixScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
