import 'package:flutter/material.dart';

class StudentsLevelFiveScreen extends StatelessWidget {
  const StudentsLevelFiveScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
